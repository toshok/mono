if CROSS_COMPILING
SUBDIRS = arch utils io-layer cil metadata mini profiler
else
SUBDIRS = arch utils io-layer cil metadata mini tests unit-tests benchmark profiler
endif
DIST_SUBDIRS = arch utils io-layer cil metadata mini tests unit-tests benchmark profiler
